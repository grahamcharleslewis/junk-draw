<%= form_with(model: week) do |form| %>
  <% if week.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(week.errors.count, "error") %> prohibited this week from being saved:</h2>

      <ul>
        <% week.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :week_no, style: "display: block" %>
    <%= form.number_field :week_no %>
  </div>

  <div>
    <%= form.label :date_commencing, style: "display: block" %>
    <%= form.date_field :date_commencing %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
